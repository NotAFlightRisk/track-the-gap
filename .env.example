# TfL Unified API key. Free from https://api-portal.tfl.gov.uk/ - the app only makes read calls.
# Without one the API still answers, but at a much lower rate limit.
TFL_APP_KEY=

# Seconds to hold a live snapshot before refetching. Two API calls per refresh, whatever the traffic.
TFL_POLL_SECONDS=15
